@model ECOE.Models.Pessoa

@{
    /**/

    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Login.cshtml";
    var Mensagem = ViewBag.Mensagem;
}

<form class="form-signin">
    <div class="text-center mb-4">
        <img class="mb-4" src="~/IMG/logo-white-fadba.png" alt="" width="200">
        @*<h1 class="h3 mb-3 font-weight-normal">Floating labels</h1>
        <p>Build form controls with floating labels via the <code>:placeholder-shown</code> pseudo-element. <a href="https://caniuse.com/#feat=css-placeholder-shown">Works in latest Chrome, Safari, and Firefox.</a></p>*@
    </div>

    <div class="form-label-group">
        <input type="email" id="inputEmail" class="form-control" placeholder="Email" required autofocus>
        <label for="inputEmail">Email</label>
    </div>

    <div class="form-label-group">
        <input type="password" id="inputPassword" class="form-control" placeholder="Senha/RA" required>
        <label for="inputPassword">Senha/RA</label>
    </div>

    @*<div class="checkbox mb-3">
        <label>
            <input type="checkbox" value="remember-me"> Remember me
        </label>
    </div>*@
    <button class="btn btn-lg btn-primary btn-block" type="submit">Entrar</button>
    <p class="mt-5 mb-3 text-muted text-center">&copy; ECOE-FADBA</p>
</form>

@*<div class="container ">
        <div class="row">
            <div class="col-lg-4 col-md-6 col-sm-8 ml-auto mr-auto">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()



                    <div class="card card-login card-hidden">
                        <div class="card-header card-header-primary text-center fundoazul">
                            <h4 class="card-title" style="font-weight: bold">ECOE</h4>
                        </div>
                        <div class="card-body ">

                            <div class="input-group has-rose">
                                <div class="input-group-prepend">
                                    <span class="input-group-text">
                                        <i class="material-icons">email</i>
                                    </span>
                                </div>
                                @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
                            </div>

                            <div class="input-group has-rose">
                                <div class="input-group-prepend">
                                    <span class="input-group-text">
                                        <i class="material-icons">lock_outline</i>
                                    </span>
                                </div>
                                @Html.PasswordFor(model => model.Senha, new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.Senha, "", new { @class = "text-danger" })
                            </div>

                        </div>
                        <div class="card-footer justify-content-center">
                            <input type="submit" value="Entrar" class="btn btn-outline-primary btn-link btn-lg">
                        </div>
                    </div>
                }
                </div>
        </div>
    </div>*@

@if (Mensagem == 2)
{

    <div class="alert alert-danger col-md-6" style=" margin:auto ;">

        <p style="font-size: 20px; text-align:center; margin-bottom:0;">Usuario ou senha Inválidos! </p>
    </div>
}
